/*package com.chordbox.models;

import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

public class OrderTest {

    private Order order;

    @BeforeEach
    public void setUp() {
        // Initialize an Order object before each test
        order = new Order();
        // Set up any initial state if needed
    }

    @Test
    public void testOrderInitialization() {
        // Assert initial state
        assertNotNull(order);
        // Add more assertions based on the initial state of your Order object
    }

    @Test
    public void testOrderTotalCalculation() {
        // Set up order details
        order.addItem(new Item("Guitar", 100)); // Assume you have an Item class
        order.addItem(new Item("Drum", 200));

        // Calculate the expected total
        double expectedTotal = 300;

        // Assert that the total calculation is correct
        assertEquals(expectedTotal, order.calculateTotal());
    }
}
 */